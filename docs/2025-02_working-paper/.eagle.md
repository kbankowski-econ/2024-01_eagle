# EAGLE WP — session handoff (`docs/2025-02_working-paper/`)

Working paper: EAGLE extended to **11 individual euro-area countries** (+ US, RW),
used to evaluate NGEU. Source: `draftPaper.tex`. This note is a handoff for AI
sessions; **it is not auto-loaded** — read it before editing. Plan (done /
remaining) first; reference material (build, pipeline, mapping) below.

**State as of last touch (2025-12-17 build, last commit 2025-11-21):** paper is
structurally complete — 61 pages, all sections + 3 appendices drafted. Work
stopped mid-polish on the NGEU figures. Uncommitted in the tree: `\date{August
2025}` → `\date{November 2025}` and the resulting PDF rebuild.

---

## ✅ DONE (2025-11)
- **NGEU inputs & charts** (2025-11-21) — bubble chart re-plotted
  (`plotNGEUbubbleChart.py` → `figures/NGEU_bubble_chart.pdf`), column-chart and
  bubble-chart input tables written to `databases/allCtryNGEUinputTable_{A,Arat}.csv`
  by `driversMatlab/processNGEUdata.m`.
- **Multiplier tables** (2025-11-13) — `tables/multiplGovSpending.tex`
  (App. C Sensitivity), generated by `tables/multiplGovSpending.m`.
- **Contribution decomposition** (2025-11-10) — `driversMatlab/investigateContributions.m`;
  `*_contributions.pdf` figures; see `investigations/contribution-rescaling.pdf`.
- **All single-country fiscal shocks run** (2025-11-09/10) — 11 countries × {gc, gi},
  driven by `driversMatlab/runAllSimul.m` over `envi.shockDict` rows 6–16.
- **EA-wide gov. investment re-run to last 3Y** (2025-11-09), IRF charts cleaned
  and fine-tuned (§3 Macroeconomic model properties).

## ⬜ REMAINING / TODO

### Blocking before any circulation
- ⬜ **9 undefined `\ref`s** — the paper still uses old-style labels that no longer
  exist. `fig1` (l.236), `fig3` (l.909), `fig4` (l.931), `fig5` (l.941), `fig6`
  (l.966), `fig8` (l.672), `finalGoodProduction`, `figApp:nationalAccountsData`,
  `figApp:govSpending`, `figApp:govTaxRates`, `diagram:ssSolutionStrategy` (×2).
  The NGEU section (`fig4`–`fig6`) should point at `fig:NGEU_input`,
  `fig:effectNGEU`, `fig:effectNGEUspillovers`.
- ⬜ **3 undefined citations** — `GomesEtAl2010ecbwp`, `GomesEtAl2012`, `FAS2025`
  missing from the shared bib (`\bibliopath`, see Files & build).
- ⬜ **Unfilled source note** — `\source{Own calculations based on ....}` on the
  government-investment import-shares figure (App. B).
- ⬜ **Commit the working tree** — the date bump + PDF are still uncommitted.

### Inline `%TODO`s left in `draftPaper.tex`
- ⬜ l.216 — check the data source.
- ⬜ l.251 — *"for Emile, check the numbers on the EA shares."*
- ⬜ l.252 — a chart with GDP shares could be nice here.
- ⬜ l.1114 — check biases/elasticities are consistent with Clancy and original EAGLE.

### Content gaps
- ⬜ **Sensitivity appendix is thin** — only `multiplGovSpending.tex` is `\input`.
  Four generated tables sit unused in `tables/`: `sensitivityShocks_{gc,gi}_EA_{y,pic4}.tex`
  and `shocksTable.tex`. Decide: wire in or delete.
- ⬜ **`effectNGEUspillovers.png` is the last PNG figure** among PDFs — regenerate
  as vector for consistency with the rest.
- ⬜ **Trade-matrix calibration** — flagged in Conclusions as the main precision
  limit; open methodology questions collected in `aMyNotes/Trade_questions.md`
  (services imports, re-export treatment, vintage mismatch across EAGLE versions).

### Housekeeping
- ⬜ **iCloud duplicate files** — the tree is full of `"<name> 2.mod"` /
  `"<name> 2.m"` / `"<name> 2.py"` shadow copies (hundreds under
  `eagleParsingTemp/modFiles/`, also in `driversPython/`, `subroutines/`,
  `+environment/`). They are not gitignored and make `ls` unreadable. Sweep them.

---

## Files & build
- **Source:** `draftPaper.tex` (single file, 93 KB). Style `ecta`.
- **Bib is external and machine-local:** `draftPaper_localBibliographyPath.tex` sets
  `\bibliopath` → `/Users/kk/Documents/0000-00_work/2021-08_literature/_literature_references`.
- **Compile:** `latexmk -pdf -cd docs/2025-02_working-paper/draftPaper.tex`
- Current build: **61 pages**, but *not* clean — see the undefined refs/citations above.
- Figures: `figures/*.pdf` (+ `.png`/`.html` siblings from Plotly). Tables:
  `tables/*.tex`, each generated by the `.m` file of the same name.

## Pipeline (MATLAB → CSV → Python charts → LaTeX)
1. `iniProject.m` — paths (`utils.call.paths`, machine-local), IRIS, **Dynare 6.1**.
2. **Steady state:** `driversMatlab/calculateSteadyState.m` walks the homotopy chain
   `steady0 → 1a → 1b → 2 → 3 → 4 → 6 → steady7`. **`steady7` is the calibrated
   terminal SS** that every table and shock reads
   (`eagleParsingTemp/modFiles/steady7/Output/steady7_results.mat`).
   Faster iteration without reloading: `eagleParsingTemp/runHomothopyValuesModify.m`.
3. **Shocks:** `driversMatlab/runAllSimul.m` → `functions.runEAGLEsimul(<shock>)`;
   single ones via `runNGEUshock.m`, `runEaWideFiscalShock.m`,
   `runEAsingleCountryShock.m`, `runEAexternalShock.m`. Shock names and captions
   live in `+environment/csvFiles/shockDict.csv` (27 entries).
4. **Tables:** `driversMatlab/runLatexCalibTables.m` (calibration tables from
   `steady7struct`) and `runLatexSimulTables.m` (multipliers) write straight into
   `docs/2025-02_working-paper/tables/`.
5. **Charts:** `runChartsForWP.m` (MATLAB, `plotting.WP.*`) and
   `driversPython/{plotIRFs,plotCalibCharts,plotTradeFlows,plotNGEUbubbleChart}.py`
   write into `docs/2025-02_working-paper/figures/`.
6. **Trade matrix:** `runTradeMatrixFromIOproject.m` / `runTradeMatrixFromXls.m` →
   `eagleParsingTemp/modFiles/TradeMatrix_calibrated_temp.m`.

## Country block ↔ code
`EAA`=RA (rest of EA) · `EAB`=AT · `EAC`=BE · `EAD`=FI · `EAE`=FR · `EAG`=NL ·
`EAH`=ES · `EAI`=GR · `EAJ`=IE · `EAK`=IT · `EAL`=PT · `EAM`=DE · plus `US`, `RW`.
(From `runTradeMatrixFromXls.m`; note `EAF` is absent from the calibrated list.)

Import items: `imcy` private consumption · `imcgy` gov. consumption ·
`imiy` private investment · `imigy` gov. investment. Table suffixes: `MC`/`MCG`/`MI`/`MIG`.

## Paper structure
§1 Introduction · §2 The model (2.1 extension · 2.2 calibration · 2.3 SS solution
strategy) · §3 Macroeconomic model properties (monetary · external · EA-wide fiscal ·
single-country fiscal) · §4 NGEU evaluation · §5 Conclusions ·
App. A Calibration data · App. B Calibration details · App. C Sensitivity analysis.

## Repo
Two remotes: `origin` and `originGitLab`, both `main`. No branches, no stashes.
